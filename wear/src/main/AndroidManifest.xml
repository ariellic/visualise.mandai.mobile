<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itpteam11.visualisemandai">

    <uses-feature android:name="android.hardware.type.watch" />

    <meta-data
        android:name="com.google.android.gms.version"
        android:value="@integer/google_play_services_version" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="android.support.wearable.activity.ConfirmationActivity"></activity>



        <activity
            android:name=".Show_StatusActivity"
            android:label="@string/title_activity_show__status" >
    </activity>
        <activity
            android:name=".ShowActivity"
            android:label="@string/title_activity_show"></activity>
        <activity android:name=".ShowDelayActivity"></activity>
        <activity android:name=".ShowCancelActivity"></activity>
        <activity android:name=".StatusActivity" />
        <activity android:name=".EscapeActivity"></activity>
        <activity
            android:name=".TramActivity"
            android:label="@string/title_activity_tram"></activity>
             <activity
            android:name=".TramStatusActivity"
            android:label="@string/title_activity_tram_status"></activity>
    </application>

</manifest>
